package 셋로또;

import java.util.HashSet;

/* public class SetLotto {

        public static void main(String[] args) {
            HashSet<Integer> list = new HashSet<>();     // AtrrayList는 배열과 다르게 크기를 지정할 필요가 없다
            int tmp;    // tmp는 로또 번호를 생성해 임시 저장하는 변수

            while (true) {
                int tmp =  (int)(Math.random() * 45) + 1;      // 1~45 사이의 랜덤한 숫자를 생성
                set.add(tmp);
            }
            if (list.size() == 6) break;

            System.out.println(set);
        }
    }
*/